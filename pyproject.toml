[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "andaltura-data-engine"
version = "0.1.0"
description = "Andaltura Data Engine"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
include = ["primer*"]